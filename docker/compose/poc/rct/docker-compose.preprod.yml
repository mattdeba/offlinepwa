version: '3.8'

services:
  tfour-rct:
    image: docker-repository.mycuma.fr/fncuma-api/tfour:latest
    container_name: tfour-rct
    environment:
      - NODE_ENV=preprod
    networks:
      - frontend-rct
    ports:
      - '8089:8080'
    restart: unless-stopped

networks:
  frontend-rct:
    external: true
